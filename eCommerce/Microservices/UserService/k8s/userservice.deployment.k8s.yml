apiVersion: apps/v1
kind: Deployment
metadata:
  name: userservice
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: userservice
  template:
    metadata:
      labels:
        app: userservice
    spec:
      containers:
        - name: userservice
          image: astmatikerne/ecommerce-user:latest
          ports:
            - containerPort: 80
